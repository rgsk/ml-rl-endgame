# Install PyTorch separately so it matches the GPU driver on the machine.
# For the current RunPod CUDA 12.8 image:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128

tiktoken
transformers
numpy
tqdm

# Optional notebook/plotting tools.
ipykernel
matplotlib
